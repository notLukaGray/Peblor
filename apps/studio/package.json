{
  "name": "@peblor/studio",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@pb/contracts": "1.0.0",
    "@pb/core": "1.0.0",
    "@pb/runtime-react": "1.0.0",
    "culori": "^4.0.2",
    "framer-motion": "^12.38.0",
    "hls.js": "^1.6.16",
    "next": "^16.2.12",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "three": "^0.184.0",
    "zod": "4.4.3"
  },
  "scripts": {
    "dev": "set -a && [ -f ../../.env ] && . ../../.env || true && set +a && export ENABLE_DEV_API=\"${ENABLE_DEV_API:-1}\" && ../../node_modules/.bin/next dev --port 3001",
    "build": "set -a && [ -f ../../.env ] && . ../../.env || true && set +a && ../../node_modules/.bin/next build",
    "start": "set -a && [ -f ../../.env ] && . ../../.env || true && set +a && ../../node_modules/.bin/next start",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
